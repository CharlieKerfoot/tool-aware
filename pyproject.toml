[project]
name = "tool-aware"
version = "0.1.0"
description = "Weekly automated digest of emerging dev tools from HN + GitHub, scored by Claude"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "anthropic",
]
